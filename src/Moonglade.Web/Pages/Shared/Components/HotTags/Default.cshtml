@model IReadOnlyList<KeyValuePair<Moonglade.Core.Tag, int>>

@foreach (var item in Model)
{
    <a asp-controller="Home" asp-action="TagList" asp-route-normalizedName="@item.Key.NormalizedName" rel="@item.Value" class="d-inline-block btn btn-sm btn-accent mb-2">@item.Key.DisplayName</a>
}