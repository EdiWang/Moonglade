@{
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
}

<h3>
    @Localizer["Diagnosis"]
</h3>
<hr />

<ul class="nav nav-tabs">
    <li role="presentation" class="nav-item nav-item-non-margin">
        <a class="nav-link @(currentAction == "About" ? "active" : null)" asp-controller="Admin" asp-action="About">@Localizer["About"]</a>
    </li>
    <li role="presentation" class="nav-item nav-item-non-margin">
        <a class="nav-link @(currentAction == "AuditLogs" ? "active" : null)" asp-controller="Admin" asp-action="AuditLogs">@Localizer["Audit Logs"]</a>
    </li>
</ul>
<div class="mb-3"></div>