@model X.PagedList.StaticPagedList<Moonglade.Core.PostDigest>

<h3>
    @Localizer["Featured Posts"]
</h3>
<hr />

@if (Model.Any())
{
    <partial name="_PostList" />
}
else
{
    <div class="alert alert-info">
        @Localizer["No Posts"]
    </div>
}

<partial name="_LightSwitch" />